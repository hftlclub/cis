var config = require('../config');

//retrieve ICS file from URL and parse events
exports.retrieveIcs = function(callback) {


        if(!rows.length) return callback(null, false);

        return callback(null, rows);
    });
}
